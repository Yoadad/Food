@model IEnumerable<Food.Models.Food>


<table>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @item.Id
            </td>
            <td>
                @item.Name
            </td>
        </tr>
    }
</table>